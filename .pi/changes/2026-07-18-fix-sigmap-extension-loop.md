# Changes: Fix sigmap extension injection loop and config (2026-07-18)

## New files
| File | Description |
|------|-------------|

## Modified files (all hunks accepted)
| File | Lines | Description |
|------|-------|-------------|
| `~/.pi/agent/extensions/sigmap.ts` | 1-130 | Removed hardcoded `[SIGMAP TEST]` context injection that caused AI loop; replaced with useful context provider that reads `query-context.md` inline; removed unused imports/functions/variables; silenced judge output |
| `gen-context.config.json` | line 9 | Added `"copilot"` to outputs array so sigmap also generates `.github/copilot-instructions.md` |

## Deleted files
| File | Description |
|------|-------------|
| `.context/` (root) | Stale directory with broken `query-context.md` paths (double `.md` extensions, wrong prefixes) |

## Root causes fixed
1. **Loop:** `context` hook injected `[SIGMAP TEST] Read .context/query-context.md first` on every prompt, forcing the AI to loop reading that file. Fixed by replacing with inline file list from `query-context.md`.
2. **Broken paths:** `query-context.md` referenced `packages/tools/.github/context-AGENTS.md.md` (double `.md` extension) — caused by sigmap judge generating bad paths from per-module context files. Removed stale file.
3. **Missing copilot output:** Config only had `outputs: ["codex"]` so `.github/copilot-instructions.md` wasn't being generated by sigmap gen-context.
